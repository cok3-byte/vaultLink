## Summary

<!-- What does this change do, and why? -->

## Checklist

- [ ] `./gradlew buildPlugin verifyPlugin` passes locally
- [ ] `CHANGELOG.md` updated under `## [Unreleased]` (if user-visible)
- [ ] No secrets, tokens, or credentials in the diff
- [ ] If this bumps the plugin version, remember to tag and push `<version>` on `main` after
  merge — merging alone doesn't publish a release (see [CONTRIBUTING.md](../CONTRIBUTING.md#releasing-a-new-version))
